-
  name: for web server
  hosts: server
  become: yes
  tasks:
-
  name: install web server
    apt:
      name: apache2
      state: present
-
  name: service start apache
    service:
    name: apache2
    enabled: yes
    state: started
